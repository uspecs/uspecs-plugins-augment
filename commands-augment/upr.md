---
description: Create a pull request (upr).
---

# upr

Parse user input as `[options]`. Identify the root directory of the plugin (PLUGIN_ROOT) and run `bash {PLUGIN_ROOT}/bin/softeng.sh action upr [options]` and follow the instructions in the output.

Options: `--no-archive`
