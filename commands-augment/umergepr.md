---
description: Merge an approved pull request (umergepr).
---

# umergepr

Identify the root directory of the plugin (PLUGIN_ROOT) and run `bash {PLUGIN_ROOT}/bin/softeng.sh action umergepr` and follow the instructions in the output.
