# Create change request file

## data

Append the following artifacts to `${change_file}`:

- `@artdef_change_why_what`
- `@artdef_change_how` (?no_impl)
- `@artdef_impl_all_sections` (?!no_impl)

Rules:

- Do not start implementation of the change request. Only create the change request file with the appropriate sections and artifacts as described above
