---
name: usync
description: Align Working Change Folder plan and specs with source changes
---

# usync

Parse user input as `[options]`.

Identify the root directory of the plugin (PLUGIN_ROOT) and run `bash {PLUGIN_ROOT}/bin/softeng.sh action usync [options]` and follow the instructions in the output.

Options: `-y`
